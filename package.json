{
  "name": "api-mocking-tests",
  "version": "1.0.0",
  "description": "Playwright API mocking tests for financial transactions",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "keywords": ["playwright", "testing", "api-mocking"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
